function BottomInput() {
  return (
    <div>
      this should contain exp date and cvc then the confirm button You could
      decide where the exp date would show on the bottom card, maybe at far left
      of the cvc?
    </div>
  );
}
export default BottomInput;
